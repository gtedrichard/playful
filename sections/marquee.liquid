<div class="mq {% if section.settings.style != 'yellow' %}mq--{{ section.settings.style }}{% endif %}" aria-hidden="true">
  <div class="mq-t">
    {%- assign reps = 2 -%}
    {%- for i in (1..reps) -%}
      {%- if section.blocks.size > 0 -%}
        {%- for block in section.blocks -%}
          <span class="mi" {{ block.shopify_attributes }}><span class="md"></span>{{ block.settings.text }}</span>
        {%- endfor -%}
      {%- else -%}
        <span class="mi"><span class="md"></span>No adhesive, ever</span>
        <span class="mi"><span class="md"></span>Swap layouts in seconds</span>
        <span class="mi"><span class="md"></span>Keep what you outgrow</span>
        <span class="mi"><span class="md"></span>Two clear sheets, one border</span>
        <span class="mi"><span class="md"></span>Stickers stay intact</span>
        <span class="mi"><span class="md"></span>Retire a layout, keep the chapter</span>
        <span class="mi"><span class="md"></span>Thin hardshell, full protection</span>
        <span class="mi"><span class="md"></span>Change freely — nothing's lost</span>
      {%- endif -%}
    {%- endfor -%}
  </div>
</div>

{% schema %}
{
  "name": "Marquee",
  "settings": [
    { "type": "select", "id": "style", "label": "Color", "options": [
      { "value": "yellow", "label": "Yellow" }, { "value": "ink", "label": "Ink" }, { "value": "blue", "label": "Blue" }
    ], "default": "yellow" }
  ],
  "blocks": [
    { "type": "item", "name": "Phrase", "settings": [ { "type": "text", "id": "text", "label": "Phrase", "default": "No adhesive, ever" } ] }
  ],
  "presets": [{ "name": "Marquee" }]
}
{% endschema %}
