<div class="pr12 rv">
  <div class="pr12-cross" aria-hidden="true"></div>
  <div class="wrap">
    <p class="pr12-text">
      <span class="prw">{{ section.settings.line1 | default: 'Designed' }}</span><br>
      <span class="prw">{{ section.settings.line2_pre | default: 'for what you' }} </span><span class="prc pr-serif">{{ section.settings.line2_accent | default: 'keep.' }}</span>
    </p>
  </div>
</div>

{% schema %}
{
  "name": "Promise",
  "settings": [
    { "type": "text", "id": "line1", "label": "Line 1", "default": "Designed" },
    { "type": "text", "id": "line2_pre", "label": "Line 2 (solid)", "default": "for what you" },
    { "type": "text", "id": "line2_accent", "label": "Line 2 (serif accent)", "default": "keep." }
  ],
  "presets": [{ "name": "Promise" }]
}
{% endschema %}
