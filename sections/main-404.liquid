<div class="err-page">
  <div class="dotgrid" aria-hidden="true"></div>
  <div class="err-code"><span>4</span><span class="e0">0</span><span>4</span></div>
  <p>This layout got retired. The page you're after isn't here — but the case still is.</p>
  <div class="row gap-3" style="flex-wrap:wrap;justify-content:center">
    <a href="{{ routes.root_url }}" class="btn btn--blue btn--lg">Back home</a>
    <a href="/products/keepkase-alpha" class="btn btn--ghost btn--lg">See the alpha</a>
  </div>
</div>

{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
